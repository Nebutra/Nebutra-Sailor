name: Deploy Carina docs (ECS)

# Rollback only. Live carina.nebutra.com is Fly (deploy-carina-fly.yml).
# Dispatch this to refresh the Shanghai nginx copy; it does not steal DNS
# unless you also flip the A record by hand.

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Carina git ref to deploy"
        required: true
        default: main
      reason:
        description: "Must contain rollback-carina-ecs to confirm this is a rollback"
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: deploy-carina-ecs
  cancel-in-progress: true

jobs:
  deploy:
    name: Build + rsync carina docs to ECS
    if: contains(github.event.inputs.reason, 'rollback-carina-ecs')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Sailor (scripts + nginx)
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Checkout Carina
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          repository: Nebutra/carina
          ref: ${{ inputs.ref || 'main' }}
          path: carina

      # The runner really does need a toolchain: the Deploy step runs
      # infra/ops/scripts/deploy-carina-docs-ecs.sh, which does `pnpm install`
      # and `pnpm run build` here and rsyncs the output — the SSH session only
      # receives the built site. The pnpm usage lives in that script rather than
      # in this file, which is easy to miss when reading the workflow alone.
      #
      # Through the shared action rather than actions/setup-node directly, which
      # is what ci-harness-closure asks for. install stays false because the
      # dependencies to install are carina's, in its own checkout, not this
      # repo's at the root.
      - name: Setup Node and pnpm
        uses: ./.github/actions/setup-node-pnpm
        with:
          node-version: "24"
          install: "false"

      # Carina builds under pnpm 11 while this repo pins 10.32.1, which is what
      # the shared action installs from the root packageManager field. corepack
      # puts the carina version in front for the build that follows.
      - name: Pin the pnpm version carina builds with
        run: |
          corepack enable
          corepack prepare pnpm@11.13.1 --activate

      - name: Configure SSH
        env:
          ECS_SSH_PRIVATE_KEY: ${{ secrets.ECS_SSH_PRIVATE_KEY }}
          ECS_SSH_KNOWN_HOSTS: ${{ secrets.ECS_SSH_KNOWN_HOSTS }}
          ECS_HOST: ${{ vars.ECS_HOST || '106.15.4.31' }}
          ECS_USER: ${{ vars.ECS_USER || 'root' }}
        run: |
          set -euo pipefail
          if [ -z "${ECS_SSH_PRIVATE_KEY:-}" ]; then
            echo "::error::ECS_SSH_PRIVATE_KEY is not set"; exit 1
          fi
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$ECS_SSH_PRIVATE_KEY" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          if [ -n "${ECS_SSH_KNOWN_HOSTS:-}" ]; then
            printf '%s\n' "$ECS_SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          else
            ssh-keyscan -H "$ECS_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          fi
          cat >> ~/.ssh/config <<EOF
          Host carina-ecs
            HostName ${ECS_HOST}
            User ${ECS_USER}
            IdentityFile ~/.ssh/id_deploy
            StrictHostKeyChecking accept-new
          EOF
          chmod 600 ~/.ssh/config

      # Same box, same default-443 301. A later main nginx.conf upload that
      # omitted this include sent kuanlan.nebutra.com to the marketing apex.
      # Run before the Carina build so a docs failure cannot skip the vhost.
      - name: Restore kuanlan nginx vhost
        env:
          REMOTE_HOST: carina-ecs
        run: bash infra/ops/scripts/install-kuanlan-nginx-vhost.sh

      - name: Smoke kuanlan host stays on itself
        run: |
          set -euo pipefail
          for path in / /create /wardrobe /moments /me; do
            loc=$(curl -sSI --max-time 15 "https://kuanlan.nebutra.com${path}" 2>/dev/null | tr -d '\r' | awk 'tolower($1)=="location:"{print $2; exit}')
            code=$(curl -sS -o /dev/null -w '%{http_code}' --max-time 20 "https://kuanlan.nebutra.com${path}" || echo 000)
            echo "path=$path code=$code location=${loc:-none}"
            case "${loc:-}" in
              https://nebutra.com|https://nebutra.com/*)
                echo "::error::$path still 301s to nebutra.com"
                exit 1
                ;;
            esac
          done

      - name: Deploy
        env:
          CARINA_REPO_DIR: ${{ github.workspace }}/carina
          REMOTE_HOST: carina-ecs
        run: bash infra/ops/scripts/deploy-carina-docs-ecs.sh

      - name: Smoke public host
        run: |
          set -euo pipefail
          for i in 1 2 3 4 5 6 7 8; do
            code=$(curl -sS -o /dev/null -w '%{http_code}' --max-time 25 "https://carina.nebutra.com/?ci=${GITHUB_SHA}" || echo 000)
            llms=$(curl -sS -o /dev/null -w '%{http_code}' --max-time 25 "https://carina.nebutra.com/llms.txt" || echo 000)
            loc=$(curl -sSI --max-time 15 "https://carina.nebutra.com/" 2>/dev/null | tr -d '\r' | awk 'tolower($1)=="location:"{print $2; exit}')
            echo "attempt $i home=$code llms=$llms location=${loc:-none}"
            if [ "$code" = "200" ] && [ "$llms" = "200" ]; then
              case "${loc:-}" in
                https://nebutra.com/*|https://nebutra.com) echo "::error::still redirecting to apex"; exit 1 ;;
              esac
              exit 0
            fi
            sleep 15
          done
          echo "::error::carina.nebutra.com not healthy (need nginx vhost + dist on ECS)"
          exit 1
