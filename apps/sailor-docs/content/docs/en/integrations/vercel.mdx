---
title: Vercel
description: Deploy the Nebutra marketing surface to Vercel and keep app/API/docs origins on the appropriate runtime boundary.
---

## Overview

Nebutra uses Vercel where it is the best runtime boundary: the public marketing
site, preview deployments, and content surfaces. Stateful app/API/docs origins
can run separately on ECS or another self-hosted target when regional control,
network policy, or origin ownership matters.

## Deploy from the monorepo

Vercel-hosted apps are deployed as separate projects:

| App | Project name |
|-----|-------------|
| `apps/landing-page` | `nebutra-landing` |
| `apps/studio` | `nebutra-studio` |

`apps/sailor-docs` powers `docs.nebutra.com` and is deployed as a standalone
Next.js app behind the ECS/Nginx origin. Use `https://nebutra.com/docs` as the
public product entrypoint; it redirects to the docs app boundary.

### Link each app

```bash
cd apps/landing-page
vercel link

cd ../web
vercel link

# Repeat for each app
```

### Set environment variables

```bash
# Pull env vars for local development
vercel env pull .env.local

# Add a new env var
vercel env add DATABASE_URL production
```

## Turborepo remote cache

Enable shared build cache across CI and local machines:

```bash
npx turbo link
```

This uses your Vercel account as the remote cache backend — cache misses are eliminated across the team.

## Environment variables

Nebutra uses these environment variables on Vercel:

```bash
# Auth
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...
CLERK_SECRET_KEY=sk_live_...
CLERK_WEBHOOK_SECRET=whsec_...

# Database
DATABASE_URL=postgresql://...

# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email
RESEND_API_KEY=re_...

# Redis
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...

# Error tracking
SENTRY_DSN=https://...

# Internal
ADMIN_API_KEY=... # min 32 chars, generate: openssl rand -hex 32
```

## Preview deployments

Every pull request gets an automatic preview URL. Vercel automatically sets the correct `VERCEL_URL` so API calls work in preview environments.

## Vercel WAF

The WAF is enabled by default for all Nebutra deployments. Custom rules are configured in `vercel.json`:

```json
{
  "firewall": {
    "rules": [
      {
        "name": "Block API abuse",
        "active": true,
        "condition": {
          "op": "and",
          "conditions": [
            { "type": "path", "op": "pre", "value": "/api/" },
            { "type": "rate_limit", "requests": 100, "window": 60 }
          ]
        },
        "action": "challenge"
      }
    ]
  }
}
```

## Related

<CardGroup cols={2}>
  <Card title="Rate Limits" icon="gauge" href="/guides/rate-limits">
    API quotas and rate limit configuration.
  </Card>
  <Card title="API Overview" icon="code" href="/api-reference/overview">
    API base URL and conventions.
  </Card>
</CardGroup>
