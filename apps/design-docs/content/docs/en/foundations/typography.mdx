---
title: "Typography"
description: "Nebutra Precision Stack — Geist Sans, Geist Mono, vivo Sans SC. Variable fonts, CJK cascade, and type scale."
---

<Callout>
  Typography specifications are sourced from the **云毓智能 品牌视觉识别手册**. Use only brand-approved typefaces.
</Callout>

## Type Hierarchy

Nebutra uses a four-tier type hierarchy. Each tier has a specific role and must not be used outside its designated context.

<TypographyHierarchyDemos />

---

## Font System — "Precision Stack"

The Nebutra design system uses three fonts. Geist handles all text roles through weight and size variation alone — no separate display typeface is needed.

| Font | Role | Variable | License |
|------|------|----------|---------|
| **Geist Sans** | UI, body, headings, display | `--font-geist-sans` | SIL OFL (Vercel) |
| **Geist Mono** | Code, data, tokens, IDs | `--font-geist-mono` | SIL OFL (Vercel) |
| **vivo Sans SC** | Chinese (CJK) UI text | `--font-vivo-sans-sc` | Licensed from vivo — see attribution below |

---

## Geist Sans — Primary Typeface

Geist Sans is the single typeface for all Nebutra UI text — from hero headlines to body copy. It is a variable font supporting weights 100 through 900, enabling precise control across every scale without switching fonts.

**Design rationale:** Geist was designed by Vercel for high-density developer products. Its geometric precision and optical sizing match the Vercel / Linear / Cursor aesthetic that Nebutra targets.

### Roles by Weight

| Weight | Value | Use Case |
|--------|-------|----------|
| Thin | 100 | Oversized decorative display at 96px+ |
| ExtraLight | 200 | Large ambient headlines |
| Light | 300 | Subtitle / tagline at hero scale |
| Regular | 400 | Body copy, descriptions, paragraph text |
| Medium | 500 | Labels, navigation items, captions |
| SemiBold | 600 | Subheadings, button labels (H3–H4) |
| Bold | 700 | Hero headlines (H1–H2), strong emphasis |
| ExtraBold | 800 | Extra emphasis on hero text |
| Black | 900 | Maximum impact display text |

### Letter Spacing

Tighten tracking at large sizes — Geist's proportions require it:

| Scale | Tracking |
|-------|----------|
| Display (72px+) | `-0.04em` |
| Heading (40–64px) | `-0.03em` |
| Subheading (24–36px) | `-0.02em` |
| Body (14–20px) | `0em` |

### Code

```css
/* Display heading */
font-family: var(--font-sans);
font-weight: 700;
letter-spacing: -0.03em;

/* Body text */
font-family: var(--font-sans);
font-weight: 400;
letter-spacing: 0;
```

```tsx
{/* Hero headline */}
<h1 className="font-sans font-bold text-heading-72 tracking-tight">
  Enterprise SaaS Platform
</h1>

{/* Body copy */}
<p className="font-sans font-normal text-copy-16">
  Nebutra transforms raw data into intelligent cloud-native outcomes.
</p>

{/* Navigation label */}
<span className="font-sans font-medium text-ui-14">
  Dashboard
</span>
```

---

## Geist Mono — Code Typeface

Geist Mono is the monospace typeface for all code, terminal output, and technical data. It is the monospace counterpart to Geist Sans, sharing the same geometric DNA.

### When to Use

- Code blocks and inline code
- Terminal / CLI output
- API endpoint displays
- Technical data values (IDs, hashes, tokens, API keys)
- Configuration examples and environment variables

### Code

```css
font-family: var(--font-mono);
font-weight: 400;
font-feature-settings: "calt" 1, "liga" 1;
```

```tsx
<code className="font-mono text-copy-14">
  const apiKey = process.env.NEBUTRA_API_KEY
</code>

<pre className="font-mono text-copy-14 bg-neutral-950 text-cyan-400 p-4 rounded-lg">
  <code>npm install @nebutra/ui</code>
</pre>
```

---

## vivo Sans SC — Chinese Typeface

vivo Sans SC is the CJK typeface for all Chinese UI text. It is **self-hosted** and
subset in `@nebutra/fonts` — Geist has no CJK coverage at all, so before this every
Chinese character fell back to whatever the OS supplied (PingFang on macOS,
Microsoft YaHei on Windows, something else on Android). No Chinese UI text should
ever depend on the OS again.

Nothing needs to opt in: the `--font-sans`, `--font-cn`, `--font-display` and
`--font-heading` token stacks all carry the face, positioned **after** Geist so
Geist keeps Latin and the numerals and only CJK falls through. The subset contains
no Latin or digit glyphs, so that holds even if a stack is written the wrong way
round. `font-cn` / `--font-cn` remain the way to opt into the CJK line-height and
word-break metrics.

### When to Use

- All Chinese body text and UI labels
- Mixed Latin + Chinese content
- Chinese marketing copy
- CJK data labels and descriptions

### Weights

Three real files ship — 400 / 500 / 600. There is no 700 file: 700 resolves to the
600 (DemiBold) face by CSS weight matching, and `font-synthesis: none` forbids faux
bold, so Chinese headings are never synthesised.

| Weight | Value | Use Case |
|--------|-------|----------|
| Regular | 400 | Chinese body copy |
| Medium | 500 | Chinese labels, navigation |
| DemiBold | 600 | Chinese headings (700 resolves here) |

### Coverage

4,282 characters: every CJK character in the repo's `zh*` message catalogs, the CJK
punctuation and fullwidth blocks wholesale, and a floor of GB2312 level-1 (3,755
常用字) so user-entered names and places don't drop out of the face mid-sentence.
Rarer characters (GB2312 level-2 and beyond) fall through to the system face on
purpose — including them would roughly double every file. Each weight is ~490 KB
and is fetched **only** when a CJK glyph actually renders: the `@font-face` rules
carry a CJK-only `unicode-range` and nothing is preloaded, so a Latin-only page
downloads none of it.

### Code

```css
font-family: var(--font-cn);
font-weight: 400;
```

```tsx
<p className="font-cn font-normal text-copy-16">
  云端聚合 · 引领未来
</p>
```

---

## Font Stacks

### CSS Custom Properties

```css
:root {
  /* Primary UI — Geist for Latin + numerals, vivo Sans SC for CJK, system CJK
     faces behind it. The ORDER is the design decision, not an accident. */
  --font-sans: var(--font-geist-sans), "Geist",
               var(--font-vivo-sans-sc, "vivo Sans SC"),
               "Noto Sans SC", "PingFang SC", "Microsoft YaHei",
               -apple-system, BlinkMacSystemFont, system-ui, sans-serif;

  /* Code — Geist Mono */
  --font-mono: var(--font-geist-mono), "Geist Mono",
               ui-monospace, SFMono-Regular, "SF Mono", Menlo, monospace;

  /* Chinese — same order as --font-sans, minus the Latin-only system faces */
  --font-cn: var(--font-geist-sans), "Geist",
             var(--font-vivo-sans-sc, "vivo Sans SC"),
             "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
}
```

### Tailwind Utility Classes

```tsx
{/* All UI text (Latin) */}
<h1 className="font-sans">Enterprise SaaS Platform</h1>

{/* Chinese text */}
<p className="font-cn">中文正文内容</p>

{/* Code / monospace */}
<code className="font-mono">const x = 1</code>
```

### Next.js Font Loading

```tsx
// app/layout.tsx
import { cjkFontClassName } from "@nebutra/fonts/next/cjk";
import { GeistSans } from "geist/font/sans";
import { GeistMono } from "geist/font/mono";
import "@nebutra/tokens/styles.css";

// Apply to <html>:
// className={`${GeistSans.variable} ${GeistMono.variable} ${cjkFontClassName}`}
//
// All three are next/font — Geist from the `geist` package, the CJK subset from
// next/font/local. Nothing is fetched from a font CDN at build time, which is also
// why this keeps working in a network-sandboxed dev server.
```

---

## Type Scale

### Heading Scale

<div className="mt-6 mb-12">
  <TypeScaleDemo scaleType="heading" />
</div>

### Copy Scale

<div className="mt-6 mb-12">
  <TypeScaleDemo scaleType="copy" />
</div>

### UI Scale

<div className="mt-6 mb-12">
  <TypeScaleDemo scaleType="ui" />
</div>

---

## CJK Rhythm

Mixed Latin + Chinese content requires special handling to maintain visual rhythm.

### The `font-cn` Class

Apply `font-cn` to any container that displays Chinese text. This switches the font stack to Noto Sans SC and adjusts:

- **Line height** increases from 1.5 to 1.75 (CJK characters need more vertical space)
- **Letter spacing** resets to 0 (CJK does not use letter-spacing)
- **Word spacing** is managed automatically by the browser for CJK

```tsx
{/* Pure Chinese content */}
<p className="font-cn text-copy-16">
  无锡云毓智能科技有限公司致力于企业级SaaS平台建设
</p>

{/* Mixed Latin + Chinese */}
<div className="font-cn">
  <p>Nebutra (云毓智能) 是一个企业级 SaaS 平台</p>
</div>
```

### Size Adjustments

CJK glyphs are optically larger than Latin characters at the same font size. Apply a slight reduction when mixing:

```css
/* Mixed content adjustment */
.font-cn {
  font-family: var(--font-cjk);
  line-height: 1.75;
  font-size-adjust: 0.52;
}
```

### Punctuation

- Use Chinese punctuation (。，！？；：""''） in Chinese sentences
- Use English punctuation in English sentences
- In mixed content, follow the language of the surrounding clause

---

## Usage Examples

### Marketing Hero

```tsx
<section className="text-center">
  <h1 className="font-sans font-bold text-heading-72 tracking-tight">
    Enterprise SaaS Platform
  </h1>
  <p className="font-sans font-normal text-copy-20 text-neutral-400 mt-6">
    Transform raw data into intelligent cloud-native outcomes.
  </p>
</section>
```

### Dashboard Heading

```tsx
<header>
  <h2 className="font-sans font-semibold text-heading-24">
    Analytics Overview
  </h2>
  <p className="font-sans text-copy-14 text-neutral-500">
    Last 30 days performance metrics
  </p>
</header>
```

### Chinese Product Card

```tsx
<div className="p-6 border rounded-xl">
  <h3 className="font-cn font-semibold text-heading-18">
    智能数据分析
  </h3>
  <p className="font-cn text-copy-14 text-neutral-500 mt-2">
    云端聚合，数据驱动决策
  </p>
</div>
```

### Code Display

```tsx
<pre className="font-mono text-copy-14 bg-neutral-950 text-cyan-400 p-4 rounded-lg">
  <code>npm install @nebutra/ui</code>
</pre>
```

---

## Do / Don't

**Do** use Geist Sans for all Latin text — both display headlines and body copy.

**Do** control hierarchy through weight (400 body → 700 headline) and size — not font switching.

**Do** use Noto Sans SC (via `font-cn`) for all Chinese text.

**Do** apply `font-cn` to containers with CJK content to activate proper line-height.

**Do** use Geist Mono for all code and technical data.

**Do** tighten letter-spacing at display sizes (`-0.03em` to `-0.04em`).

**Don't** use non-brand fonts (Inter, Poppins, Roboto, Arial, SF Pro) in Nebutra products.

**Don't** use a separate "display" font family — Geist Sans handles all scales through weight variation.

**Don't** set letter-spacing on body text — tracking adjustments belong only to headings and display text.

**Don't** mix `font-sans` and `font-cn` on the same element — choose one based on the content language.

---

## Third-party font attribution

本软件使用了 **vivo Sans** 字体。/ This software uses the **vivo Sans** typeface.

Required by clause 2.1 of the vivo Sans 字体知识产权许可协议 (您应在软件中特别注明使用了
vivo Sans 字体). vivo Sans is licensed from vivo Mobile Communication Co., Ltd.; the
agreement is committed at `packages/design/fonts/vendor/vivo-sans/LICENCE-vivo-Sans.txt`
and it does **not** fall under the MIT licence covering Nebutra's own code. Geist and
Geist Mono are SIL OFL (Vercel).
