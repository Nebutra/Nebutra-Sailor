---
title: "Input"
description: "Single-line text input with strict labels, semantic error states, affixes, clearable search, and shortcut hints."
---

import { ComponentPreview } from "@/components/component-preview";
import { InputBasicDemo } from "@/components/previews/input-basic-demo";
import { InputClearableDemo } from "@/components/previews/input-clearable-demo";
import { InputDemo } from "@/components/previews/input-demo";
import { InputErrorDemo } from "@/components/previews/input-error-demo";
import { InputPasswordRevealDemo } from "@/components/previews/input-password-reveal-demo";
import { Input2Demo } from "@/components/previews/input-2-demo";
import { InputWithAddonsDemo } from "@/components/previews/input-with-addons-demo";
import { InputWithIconDemo } from "@/components/previews/input-with-icon-demo";
import { InputWithLabelDemo } from "@/components/previews/input-with-label-demo";

<ComponentPreview name="input-basic-demo" className="max-w-[350px]">
  <InputBasicDemo />

  ```tsx
<Input aria-label="Project name" placeholder="my-awesome-project" />
  ```
</ComponentPreview>

## Usage

Use `Input` for one line of free-form text: names, domains, tokens, URLs, and scoped search. Switch to `Textarea` when content can wrap. Use `Combobox` when the value comes from a known list.

```tsx
<Input
  id="project-name"
  label="Project Name"
  placeholder="my-awesome-project"
  description="Use lowercase letters, numbers, and hyphens."
/>
```

## Examples

### Sizes

<ComponentPreview name="input-demo" className="max-w-[350px]">
  <InputDemo />
</ComponentPreview>

### Label And Helper Text

<ComponentPreview name="input-with-label-demo" className="max-w-[350px]">
  <InputWithLabelDemo />
</ComponentPreview>

### Error

<ComponentPreview name="input-error-demo" className="max-w-[350px]">
  <InputErrorDemo />
</ComponentPreview>

### Prefix And Suffix

<ComponentPreview name="input-with-addons-demo" className="max-w-[350px]">
  <InputWithAddonsDemo />
</ComponentPreview>

Text affixes such as `https://` and `USD` are wired through `aria-describedby`. Icon affixes are decorative, so pass `aria-hidden` on the icon.

### Search

<ComponentPreview name="input-with-icon-demo" className="max-w-[350px]">
  <InputWithIconDemo />
</ComponentPreview>

`type="search"` clears the current value with Escape when the field is dirty. Use a scoped placeholder such as `Search projects`, not a generic `Search`.

### Clearable Search

<ComponentPreview name="input-2-demo" className="max-w-[350px]">
  <Input2Demo />
</ComponentPreview>

Pair `type="search"` with `clearable` when the field filters an in-place list and needs a one-click reset. Keep the icon decorative and let the scoped `aria-label` provide the accessible name.

### File

Use `type="file"` for native uploads when the browser file picker is the right interaction. The file selector button inherits the same height, border, and typography scale as the rest of `Input`; avoid styling it as a custom upload zone unless the workflow needs drag-and-drop, progress, or validation previews.

### Clearable

<ComponentPreview name="input-clearable-demo" className="max-w-[350px]">
  <InputClearableDemo />
</ComponentPreview>

Use `onValueChange` for controlled clearable inputs when you only need the next string value. Use native `onChange` when the event matters.

### Password Reveal

<ComponentPreview name="input-password-reveal-demo" className="max-w-[350px]">
  <InputPasswordRevealDemo />
</ComponentPreview>

Use `revealable` on `type="password"` when the field needs a visibility toggle. The control is a tokenized button owned by `Input`, so focus rings, icon sizing, padding, and labels stay consistent with clear/search controls.

## Props

`Input` extends native `React.InputHTMLAttributes<HTMLInputElement>`, except `prefix` and `size` are owned by the component API.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `"sm" \| "md" \| "lg"` | `"md"` | Visual density tied to component tokens. |
| `label` | `string` | - | Visible label. Requires `id` at type level. |
| `id` | `string` | - | Required when `label` is provided. |
| `description` | `string` | - | Helper text wired through `aria-describedby`. |
| `error` | `string \| boolean` | - | Marks invalid state. A string also renders an inline error. |
| `prefix` | `ReactNode` | - | Non-interactive left affix. |
| `suffix` | `ReactNode` | - | Non-interactive right affix. Hidden by clear/shortcut/loading controls. |
| `clearable` | `boolean` | `false` | Shows a clear button when the field has a value. |
| `onClear` | `() => void` | - | Fires after the field is cleared. |
| `onValueChange` | `(value: string) => void` | - | Value-first callback for controlled inputs. |
| `shortcut` | `string` | - | Visual shortcut hint. Dirty fields show `Esc`. |
| `loading` | `boolean` | `false` | Shows a spinner without removing focusability. |
| `revealable` | `boolean` | `false` | Adds a password visibility button when `type="password"`. |
| `revealLabel` | `string` | `"Show password"` | Accessible label for the hidden-password state. |
| `hideLabel` | `string` | `"Hide password"` | Accessible label for the visible-password state. |
| `clearOnEscape` | `boolean` | `type === "search"` | Clears the field on Escape when dirty. |
| `wrapperClassName` | `string` | - | Applies to the control wrapper when affixes are present. |
| `fieldClassName` | `string` | - | Applies to the label/helper/error wrapper. |

## Design Contract

- Labels are short product nouns: Project Name, API Key Name, Custom Domain, Webhook URL.
- Placeholders show realistic example values, never instructions. Use `nebutra-control-plane`, `api.nebutra.com`, or `sk_live_...`, not `Enter a value`.
- Validate on blur for ordinary fields and on submit for server-backed uniqueness checks. Pass a string to `error` once the message is ready.
- Trim leading and trailing whitespace before submit in the calling form, except for secrets where whitespace may be meaningful.
- Keep the input focusable while saving; use `loading` instead of disabling unless the value is locked by role, plan, or system ownership.
- Do not use `suffix` for buttons. Use built-in controls such as `clearable` and `revealable`, or compose a dedicated field primitive when the right side needs custom interaction.

### State Matrix

| State | Product use | Contract |
|---|---|---|
| Empty | New project, custom domain, invite email | Visible label provides the name; placeholder is only an example. |
| Filled | Editing tenant metadata or API key label | Preserve user text exactly while focused; format only after blur or submit. |
| Focus | Keyboard editing in dense settings forms | Tokenized ring wraps the rounded control and never falls back to a square browser outline. |
| Loading | Checking domain availability or validating an invite | Spinner does not steal focus or replace the input value. |
| Error | Duplicate slug, invalid email, unsupported region code | `error` text names the field and constraint and is linked by `aria-describedby`. |
| Disabled | Role-locked organization slug or system-owned URL | Use disabled only when editing is impossible; explain the lock nearby. |

## Accessibility

- `label` cannot compile without `id`, preserving the label association.
- `description`, string text affixes, and string errors are merged into `aria-describedby`.
- Error text renders with `role="alert"`.
- Prefix and suffix icons are decorative; hide them from assistive technology.
- Search inputs clear with Escape only while dirty, so Escape can still close an outer dialog when the field is empty.
- Password reveal exposes `aria-pressed` and restores focus to the input after toggling.
- Focus treatment is part of the primitive contract: keyboard and pointer focus use the same tokenized ring, including error focus, clearable search, and password reveal controls.

## Tokens

`Input` consumes `inputTokens` for size, radius, padding, affix offsets, control sizes, icon sizes, and focus ring width. Runtime colors use semantic tokens: `background`, `foreground`, `input`, `ring`, `muted-foreground`, and `destructive`.

## Real Product Examples

- Project slug field in onboarding, with helper text explaining lowercase letters, numbers, and hyphens.
- API key name field in developer settings, with blur validation and server-side duplicate-name error text.
- Audit log search scoped to users, actions, and resource IDs, with a prefix icon, `clearable`, and an `Esc` hint.
- Password or secret input in account security, using `revealable` and preserving focus after toggling visibility.

## Anti-patterns

- Do not use Input for multi-line content. Use Textarea.
- Do not put actionable controls in both suffix and clear/reveal slots at the same time.
- Do not validate only through color; provide `error` text linked by `aria-describedby`.
- Do not use placeholder text as the only label in settings, billing, or IAM flows.
- Do not disable the field during async validation; keep it editable and show `loading`.
