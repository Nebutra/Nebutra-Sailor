---
title: "图表"
description: "基于 Recharts 的数据可视化模式，并内置主题感知样式。"
---

## 何时使用

当定量数据需要借助视觉比较才能更快理解时，使用图表模式，例如时间序列趋势、比例拆分或多序列对比。

以下场景使用图表：
- 数据至少有 3 个数据点，并存在比较轴（时间或类别）
- 洞察是关系型的，例如“X 增长快于 Y”
- 如果用表格展示，同样数据需要用户心算，而图表可以消除这一步

以下场景不要使用图表：
- 单个数字或 `CounterLabel` 已经能表达同样信息
- 数据是静态参考资料
- 用户需要精确值（应搭配表格，或直接使用表格）

## 结构

```
ChartContainer (owns ChartConfig, sets aspect ratio)
  └── ResponsiveContainer (fills parent width)
       └── BarChart | LineChart | AreaChart | PieChart
            ├── CartesianGrid (optional, muted stroke)
            ├── XAxis + YAxis (both optional for sparklines)
            ├── ChartTooltip + ChartTooltipContent
            ├── ChartLegend + ChartLegendContent (multi-series only)
            └── Bar | Line | Area (one per data series)
```

每个图表都应该由 `ChartContainer` 包裹。它把 `ChartConfig` 注入 context，为每个数据序列设置 CSS 自定义属性，并默认约束为 `aspect-video` 比例。

## 组合规则

### ChartConfig 配置

每个数据序列定义一个条目。将每个 key 映射到 label，以及颜色 token 或 theme object。

```tsx
import { ChartConfig } from "@nebutra/ui/primitives";

const chartConfig: ChartConfig = {
  apiCalls: {
    label: "API Calls",
    color: "hsl(var(--chart-1))",
  },
  tokens: {
    label: "AI Tokens",
    color: "hsl(var(--chart-2))",
  },
} satisfies ChartConfig;
```

支持主题感知颜色（light / dark 变体）：

```tsx
const chartConfig: ChartConfig = {
  revenue: {
    label: "Revenue",
    theme: {
      light: "#0070F3",
      dark: "#3291FF",
    },
  },
};
```

### ChartContainer 容器

```tsx
import {
  ChartContainer,
  ChartTooltip,
  ChartTooltipContent,
} from "@nebutra/ui/primitives";
import { BarChart, Bar, CartesianGrid, XAxis } from "recharts";

<ChartContainer config={chartConfig} className="h-[300px]">
  <BarChart data={data}>
    <CartesianGrid vertical={false} />
    <XAxis dataKey="month" tickLine={false} axisLine={false} />
    <ChartTooltip content={<ChartTooltipContent />} />
    <Bar dataKey="apiCalls" fill="var(--color-apiCalls)" radius={4} />
  </BarChart>
</ChartContainer>
```

### Tooltip 提示

`ChartTooltipContent` 会自动从 `ChartConfig` 读取 label 和颜色。折线图传入 `indicator="line"`，柱状图使用默认的 `indicator="dot"`。

### Legend 图例

当图表包含两个或更多序列时，添加 `ChartLegend` 和 `ChartLegendContent`。横向柱状图与折线图建议把图例放在 `verticalAlign="bottom"`；单序列图表省略图例。

## 示例
### 单序列柱状图（月度 API 调用）

```tsx
const data = [
  { month: "Jan", apiCalls: 1200 },
  { month: "Feb", apiCalls: 1800 },
  { month: "Mar", apiCalls: 1500 },
];

const config: ChartConfig = {
  apiCalls: { label: "API Calls", color: "hsl(var(--chart-1))" },
};

<ChartContainer config={config} className="h-[240px]">
  <BarChart data={data}>
    <CartesianGrid vertical={false} />
    <XAxis dataKey="month" tickLine={false} axisLine={false} />
    <ChartTooltip content={<ChartTooltipContent />} />
    <Bar dataKey="apiCalls" fill="var(--color-apiCalls)" radius={4} />
  </BarChart>
</ChartContainer>
```

### 多序列折线图

```tsx
const config: ChartConfig = {
  apiCalls: { label: "API Calls", color: "hsl(var(--chart-1))" },
  tokens:   { label: "AI Tokens", color: "hsl(var(--chart-2))" },
};

<ChartContainer config={config} className="h-[300px]">
  <LineChart data={data}>
    <CartesianGrid strokeDasharray="3 3" />
    <XAxis dataKey="month" />
    <ChartTooltip content={<ChartTooltipContent indicator="line" />} />
    <ChartLegend content={<ChartLegendContent />} />
    <Line type="monotone" dataKey="apiCalls" stroke="var(--color-apiCalls)" />
    <Line type="monotone" dataKey="tokens"   stroke="var(--color-tokens)" />
  </LineChart>
</ChartContainer>
```

## 相关组件

- [Metric Card](/zh/docs/fragment-components/metric-card) — 在 sparkline 上方展示单个 KPI
- [Logs Bar Chart](/zh/docs/fragment-components/logs-bar-chart) — 预组合的日志量柱状图片段
- [Card](/zh/docs/components/card) — 将图表包裹在卡片中，保持一致 padding 与边框
- [Skeleton](/zh/docs/components/skeleton) — 图表数据加载期间使用
