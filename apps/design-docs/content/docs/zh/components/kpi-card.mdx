---
title: KPI Card
description: 用于 dashboard 的 KPI 卡片，包含数值、图标、趋势和可选描述。
---

## 演示

<ComponentPreview name="kpi-card-demo" className="w-full" />

## 安装

```tsx
import { KpiCard } from "@nebutra/ui/primitives";
```

或通过 registry 安装 standalone copy：

```bash
npx shadcn@latest add https://ui.nebutra.com/r/kpi-card.json
```

## 用法

`KpiCard` 是紧凑的 dashboard 统计卡片。适合展示一个关键数值、一个图标，以及可选的周同比趋势。

```tsx
import { ChartTrendingUp } from "@nebutra/icons";
import { KpiCard } from "@nebutra/ui/primitives";

<KpiCard
  title="Active tenants"
  value={1284}
  icon={<ChartTrendingUp className="size-5" />}
  trend={{ value: 12, isPositive: true }}
  description="Enterprise workspaces only"
/>
```

## 何时使用

- 用于 dashboard 顶部三到六个可比较指标。
- 详情页里的 key/value 元数据优先使用 `Description`。
- 如果用户需要趋势形状，而不仅是趋势方向，使用 chart component。

## 状态

| 状态 | 契约 |
|------|------|
| 正向趋势 | 使用上升图标和正向语义色表示指标改善。 |
| 负向趋势 | 使用下降图标和 destructive 语义色表示指标变差。 |
| 持平趋势 | 当 `trend.value` 为 `0` 时使用中性表达。 |
| 无趋势 | 只展示值和可选描述，不编造变化。 |

## 属性

| 属性 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `title` | `string` | — | 简短指标名称。 |
| `value` | `string \| number` | — | 主数值。数字会被 locale-format。 |
| `icon` | `ReactNode` | — | 装饰性指标图标。 |
| `trend` | `{ value: number; isPositive: boolean }` | — | 以升/降/持平形式展示的百分比趋势。 |
| `description` | `string` | — | 趋势下方的补充说明。 |
| `className` | `string` | — | 外层 card class。 |

## 可访问性

- 指标值不能依赖趋势颜色才能理解。
- 图标是装饰性的；指标 title 和 value 必须是文本。
- 多个 KPI 卡片一起出现时，外层应有 heading 或 dashboard section label。
- 不要让整张卡片成为唯一导航入口，除非有明确的无障碍标签。

## 设计契约

- `KpiCard` 用于顶层摘要指标，不用于详细分析。
- 一行放三到六张卡，帮助用户快速比较运营健康度。
- 趋势只是辅助信号；如果用户需要趋势形状，用 `Chart`。
- 图标保持克制并承载指标类别，不做纯装饰。

## Storybook / Registry

- Storybook source: `packages/design/ui/src/primitives/kpi-card.stories.tsx`。
- Registry manifest: `/r/kpi-card.json`。

## 治理备注

这是已经进入 registry 的 dashboard primitive；在剩余通用 card spacing 和趋势语义色迁移到组件 token 前保持 `experimental`。
