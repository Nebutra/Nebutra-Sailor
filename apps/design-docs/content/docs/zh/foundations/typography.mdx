---
title: "排版与字体"
description: "云毓智能精密字体栈 — Geist Sans、Geist Mono、vivo Sans SC。可变字体、中文字体级联与字号比例尺。"
---

<Callout>
  本排版规范源自**云毓智能品牌视觉识别手册**。所有产品界面仅可使用品牌认可的字体。
</Callout>

## 字体层级

Nebutra 使用四级字体层级，每一级有固定的使用场景，不得混用。

<TypographyHierarchyDemos />

---

## 字体系统 — "精密字体栈"（Precision Stack）

Nebutra 设计系统使用三款字体。Geist 通过字重与字号的变化覆盖所有文本角色，无需额外的展示字体。

| 字体 | 职责 | CSS 变量 | 授权 |
|------|------|----------|------|
| **Geist Sans** | UI、正文、标题、展示 | `--font-geist-sans` | SIL OFL（Vercel） |
| **Geist Mono** | 代码、数据、Token、ID | `--font-geist-mono` | SIL OFL（Vercel） |
| **vivo Sans SC** | 中文（CJK）UI 文字 | `--font-vivo-sans-sc` | vivo 授权 — 见文末字体署名 |

---

## Geist Sans — 主字体

Geist Sans 是 Nebutra 所有 UI 文字的唯一英文字体——从英雄标题到正文均使用同一字体，通过字重和字号营造层级，无需切换字体。

**设计理念：** Geist 由 Vercel 为高密度开发者产品设计。其几何精度与光学尺寸调节，完全契合 Vercel / Linear / Cursor 的美学定位。

### 字重角色

| 字重 | 值 | 使用场景 |
|------|----|----------|
| Thin | 100 | 96px+ 超大装饰展示文字 |
| ExtraLight | 200 | 大型氛围标题 |
| Light | 300 | Hero 副标题 / 标语 |
| Regular | 400 | 正文、描述、段落 |
| Medium | 500 | 标签、导航、说明文字 |
| SemiBold | 600 | 次级标题、按钮文字（H3–H4） |
| Bold | 700 | 英雄标题（H1–H2）、强调 |
| ExtraBold | 800 | 特殊强调展示文字 |
| Black | 900 | 最大视觉冲击的展示文字 |

### 字间距

大字号需收紧字距——Geist 的比例要求如此：

| 级别 | 字间距 |
|------|--------|
| 展示（72px+） | `-0.04em` |
| 标题（40–64px） | `-0.03em` |
| 副标题（24–36px） | `-0.02em` |
| 正文（14–20px） | `0em` |

### 代码示例

```css
/* 展示标题 */
font-family: var(--font-sans);
font-weight: 700;
letter-spacing: -0.03em;

/* 正文 */
font-family: var(--font-sans);
font-weight: 400;
letter-spacing: 0;
```

```tsx
{/* 英雄标题 */}
<h1 className="font-sans font-bold text-heading-72 tracking-tight">
  Enterprise SaaS Platform
</h1>

{/* 正文 */}
<p className="font-sans font-normal text-copy-16">
  Nebutra transforms raw data into intelligent cloud-native outcomes.
</p>

{/* 导航标签 */}
<span className="font-sans font-medium text-ui-14">
  Dashboard
</span>
```

---

## Geist Mono — 代码字体

Geist Mono 是所有代码、终端输出和技术数据展示的等宽字体，与 Geist Sans 同源，保持一致的几何美感。

### 使用场景

- 代码块与内联代码
- 终端 / CLI 输出
- API 端点展示
- 技术数据值（ID、哈希、Token、API Key）
- 配置示例与环境变量

### 代码示例

```css
font-family: var(--font-mono);
font-weight: 400;
font-feature-settings: "calt" 1, "liga" 1;
```

```tsx
<code className="font-mono text-copy-14">
  const apiKey = process.env.NEBUTRA_API_KEY
</code>

<pre className="font-mono text-copy-14 bg-neutral-950 text-cyan-400 p-4 rounded-lg">
  <code>npm install @nebutra/ui</code>
</pre>
```

---

## vivo Sans SC — 中文字体

vivo Sans SC 是所有中文 UI 文字的字体，由 `@nebutra/fonts` **自托管**并做了子集化。
Geist 完全不含 CJK 字形，在此之前每一个汉字都落回操作系统自带字体——macOS 是苹方、
Windows 是微软雅黑、Android 又是另一套。中文界面不该再由操作系统决定长什么样。

无需逐处接入：`--font-sans`、`--font-cn`、`--font-display`、`--font-heading` 四个 token
栈都已带上这个字体，且都排在 Geist **之后**——拉丁字母与数字仍归 Geist（等宽数字、更紧的
x-height 是密集表格需要的），只有 CJK 会落到 vivo Sans SC。子集里没有任何拉丁与数字字形，
所以即使某处字体栈写反了，这条边界依然成立。`font-cn` / `--font-cn` 的作用是切换中文的
行高与断行度量。

### 使用场景

- 所有中文正文与 UI 标签
- 中英混排内容
- 中文营销文案
- CJK 数据标签与描述文字

### 字重

只有 400 / 500 / 600 三个真实文件，没有 700：按 CSS 字重匹配规则 700 会落到 600
（DemiBold）这一档，而全局的 `font-synthesis: none` 禁止伪粗体，所以中文标题不会被合成。

| 字重 | 值 | 使用场景 |
|------|----|----------|
| Regular | 400 | 中文正文 |
| Medium | 500 | 中文标签、导航 |
| DemiBold | 600 | 中文标题（700 落到这一档） |

### 覆盖范围

4,282 字：仓库内所有 `zh*` 文案目录里出现的汉字、CJK 标点与全角区整块收录，再加一层
GB2312 一级字（3,755 个常用字）作为底线——产品界面要渲染用户数据，人名地名中的某一个字
落回苹方，比不用这个字体更难看。更生僻的字（GB2312 二级及以上）有意留给系统字体：全收
会让每个文件体积翻倍。每个字重约 490 KB，且**只有真正渲染 CJK 字形时才会下载**：
`@font-face` 上的 `unicode-range` 只含 CJK 区段，且不做 preload，纯拉丁页面一个字节都不下。

### 代码示例

```css
font-family: var(--font-cn);
font-weight: 400;
```

```tsx
<p className="font-cn font-normal text-copy-16">
  云端聚合 · 引领未来
</p>
```

---

## 字体栈

### CSS 自定义属性

```css
:root {
  /* 主 UI — 拉丁与数字归 Geist，CJK 归自托管的 vivo Sans SC，系统中文字体兜底。
     顺序本身就是设计决策，不是随手写的。 */
  --font-sans: var(--font-geist-sans), "Geist",
               var(--font-vivo-sans-sc, "vivo Sans SC"),
               "Noto Sans SC", "PingFang SC", "Microsoft YaHei",
               -apple-system, BlinkMacSystemFont, system-ui, sans-serif;

  /* 代码 — Geist Mono */
  --font-mono: var(--font-geist-mono), "Geist Mono",
               ui-monospace, SFMono-Regular, "SF Mono", Menlo, monospace;

  /* 中文 — 与 --font-sans 同序，去掉只含拉丁的系统字体 */
  --font-cn: var(--font-geist-sans), "Geist",
             var(--font-vivo-sans-sc, "vivo Sans SC"),
             "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
}
```

### Tailwind 工具类

```tsx
{/* 所有 UI 文字（拉丁） */}
<h1 className="font-sans">Enterprise SaaS Platform</h1>

{/* 中文文字 */}
<p className="font-cn">中文正文内容</p>

{/* 代码 / 等宽 */}
<code className="font-mono">const x = 1</code>
```

### Next.js 字体加载

```tsx
// app/layout.tsx
import { cjkFontClassName } from "@nebutra/fonts/next/cjk";
import { GeistSans } from "geist/font/sans";
import { GeistMono } from "geist/font/mono";
import "@nebutra/tokens/styles.css";

// 应用到 <html>：
// className={`${GeistSans.variable} ${GeistMono.variable} ${cjkFontClassName}`}
//
// 三者都走 next/font：Geist 来自 geist 包，中文子集走 next/font/local。
// 构建期不请求任何字体 CDN，所以在断网沙箱里的 dev server 同样能跑。
```

---

## 字号比例尺

### 标题比例尺

<div className="mt-6 mb-12">
  <TypeScaleDemo scaleType="heading" />
</div>

### 正文比例尺

<div className="mt-6 mb-12">
  <TypeScaleDemo scaleType="copy" />
</div>

### UI 比例尺

<div className="mt-6 mb-12">
  <TypeScaleDemo scaleType="ui" />
</div>

---

## 中文排版节奏

中英混排内容需要特殊处理以维持视觉节奏。

### `font-cn` 类

对任何含中文的容器应用 `font-cn`，切换字体栈至 Noto Sans SC，并自动调整：

- **行高**从 1.5 增加至 1.75（CJK 字符需要更多垂直空间）
- **字间距**重置为 0（CJK 不使用字间距）
- **词间距**由浏览器自动处理

```tsx
{/* 纯中文内容 */}
<p className="font-cn text-copy-16">
  无锡云毓智能科技有限公司致力于企业级SaaS平台建设
</p>

{/* 中英混排 */}
<div className="font-cn">
  <p>Nebutra（云毓智能）是一个企业级 SaaS 平台</p>
</div>
```

### 尺寸调整

CJK 字形在相同字号下视觉上比拉丁字母大，混排时需轻微缩小：

```css
/* 混排内容调整 */
.font-cn {
  font-family: var(--font-cjk);
  line-height: 1.75;
  font-size-adjust: 0.52;
}
```

### 标点使用

- 中文句子使用中文标点（。，！？；：""''）
- 英文句子使用英文标点
- 混排内容以所在语言的标点规则为准

---

## 使用示例

### 营销 Hero

```tsx
<section className="text-center">
  <h1 className="font-sans font-bold text-heading-72 tracking-tight">
    Enterprise SaaS Platform
  </h1>
  <p className="font-sans font-normal text-copy-20 text-neutral-400 mt-6">
    Transform raw data into intelligent cloud-native outcomes.
  </p>
</section>
```

### 控制台标题

```tsx
<header>
  <h2 className="font-sans font-semibold text-heading-24">
    Analytics Overview
  </h2>
  <p className="font-sans text-copy-14 text-neutral-500">
    Last 30 days performance metrics
  </p>
</header>
```

### 中文产品卡片

```tsx
<div className="p-6 border rounded-xl">
  <h3 className="font-cn font-semibold text-heading-18">
    智能数据分析
  </h3>
  <p className="font-cn text-copy-14 text-neutral-500 mt-2">
    云端聚合，数据驱动决策
  </p>
</div>
```

### 代码展示

```tsx
<pre className="font-mono text-copy-14 bg-neutral-950 text-cyan-400 p-4 rounded-lg">
  <code>npm install @nebutra/ui</code>
</pre>
```

---

## 规范与禁忌

**应该** 对所有拉丁文字使用 Geist Sans——展示标题与正文均适用。

**应该** 通过字重（400 正文 → 700 标题）和字号控制层级，而非切换字体。

**应该** 对所有中文文字使用 Noto Sans SC（通过 `font-cn`）。

**应该** 对含 CJK 内容的容器应用 `font-cn`，以激活正确的行高。

**应该** 对所有代码和技术数据使用 Geist Mono。

**应该** 在展示字号下收紧字间距（`-0.03em` 至 `-0.04em`）。

**不应该** 使用非品牌字体（Inter、Poppins、Roboto、Arial、SF Pro）于 Nebutra 产品。

**不应该** 为"展示"使用独立字体家族——Geist Sans 通过字重变化覆盖所有级别。

**不应该** 对正文设置字间距——字间距调整仅适用于标题和展示文字。

**不应该** 在同一元素上混用 `font-sans` 与 `font-cn`——根据内容语言选择其一。

---

## 字体署名（Third-party font attribution）

本软件使用了 **vivo Sans** 字体。

这是 vivo Sans 字体知识产权许可协议第 2.1 条的要求（"您应在软件中特别注明使用了
vivo Sans 字体"）。vivo Sans 由 vivo 移动通信有限公司授权，协议原文提交在
`packages/design/fonts/vendor/vivo-sans/LICENCE-vivo-Sans.txt`，**不**在覆盖 Nebutra
自有代码的 MIT 许可范围内。Geist 与 Geist Mono 为 SIL OFL（Vercel）。
