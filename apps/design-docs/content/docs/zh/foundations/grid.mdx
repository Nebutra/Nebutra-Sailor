---
title: "网格与布局"
description: "Nebutra 产品通用的响应式列网格、间距尺度和容器宽度。"
---

## 布局网格

所有 Nebutra 产品 UI 都使用带固定 gutter 的 **12 列流式网格**。列数会在断点处收缩，以适配响应式布局。

| 断点 | 名称 | 最小宽度 | 列数 | Gutter | Margin |
|-----------|------|-----------|---------|--------|--------|
| `sm` | Mobile | 640px | 4 | 16px | 16px |
| `md` | Tablet | 768px | 8 | 24px | 24px |
| `lg` | Desktop | 1024px | 12 | 24px | 32px |
| `xl` | Wide | 1280px | 12 | 32px | 40px |
| `2xl` | Ultra | 1536px | 12 | 32px | 48px |

---

## 容器宽度

标准页面容器会把内容限制在可读行宽内。

```tsx
// 全宽页面 wrapper
<div className="mx-auto w-full max-w-screen-xl px-4 md:px-6 lg:px-8">

// 正文 / 文档
<div className="mx-auto max-w-3xl px-4">

// Dashboard 内容
<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">

// 窄表单 / Modal 内容
<div className="mx-auto max-w-md">
```

| 名称 | 最大宽度 | CSS 变量 | 用途 |
|------|-----------|-------------|-------|
| `max-w-sm` | 384px | — | 窄卡片、Modal |
| `max-w-md` | 448px | — | 表单、Dialog |
| `max-w-lg` | 512px | — | 宽 Modal |
| `max-w-2xl` | 672px | — | 正文、文档 |
| `max-w-4xl` | 896px | `--container-text` | 文本聚焦区域（Hero 文案、CTA、FAQ） |
| `max-w-6xl` | 1152px | `--container-content` | Dashboard 布局、定价、架构 |
| `max-w-[1400px]` | 1400px | `--container-wide` | Landing page 区块、Feature Bento、导航栏 |

---

## 间距尺度

间距尺度遵循 Tailwind 默认 4px 基准单位。margin、padding 和 gap 都应保持一致使用。

| Class | 值 | 用途 |
|-------|-------|-------|
| `space-1` | 4px | 紧凑图标间距、行内元素 |
| `space-2` | 8px | 内边距、Badge 间距 |
| `space-3` | 12px | 表单元素内部 padding |
| `space-4` | 16px | 小卡片 padding、区块间距 |
| `space-6` | 24px | 中卡片 padding、网格间距 |
| `space-8` | 32px | 大卡片 padding、区块间距 |
| `space-12` | 48px | 页面区块分隔 |
| `space-16` | 64px | Hero 区块 padding |
| `space-24` | 96px | 大区块间隔 |

---

## 常见布局模式

### 页面外壳

```tsx
<div className="min-h-screen bg-background">
  <header className="sticky top-0 z-50 border-b border-border bg-background/80 backdrop-blur">
    <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <nav className="flex h-16 items-center justify-between">
        {/* 导航内容 */}
      </nav>
    </div>
  </header>

  <main className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
    {/* 页面内容 */}
  </main>
</div>
```

### 仪表盘网格

```tsx
{/* 统计行：4 张等宽卡片 */}
<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
  <Card variant="elevated" padding="md">...</Card>
  <Card variant="elevated" padding="md">...</Card>
  <Card variant="elevated" padding="md">...</Card>
  <Card variant="elevated" padding="md">...</Card>
</div>

{/* 主内容 + 侧边栏：2/3 + 1/3 */}
<div className="mt-6 grid gap-6 lg:grid-cols-3">
  <div className="lg:col-span-2">
    {/* 主内容 */}
  </div>
  <div>
    {/* 侧边栏 */}
  </div>
</div>
```

### 功能区块（三列）

```tsx
<section className="py-16">
  <div className="mx-auto max-w-6xl px-4">
    <h2 className="text-3xl font-semibold text-center mb-12">Features</h2>
    <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {features.map((f) => (
        <Card key={f.title} variant="bordered" padding="lg">
          <Card.Header>
            <Card.Icon size="md">{f.icon}</Card.Icon>
            <Card.Title>{f.title}</Card.Title>
          </Card.Header>
          <Card.Body>
            <Card.Description>{f.description}</Card.Description>
          </Card.Body>
        </Card>
      ))}
    </div>
  </div>
</section>
```

### 居中认证 / 引导

```tsx
<div className="flex min-h-screen items-center justify-center bg-background px-4">
  <div className="w-full max-w-md">
    <Card variant="elevated" padding="lg">
      {/* 表单内容 */}
    </Card>
  </div>
</div>
```

### 分栏布局（营销 / 认证）

```tsx
<div className="grid min-h-screen lg:grid-cols-2">
  {/* 左侧：品牌面板 */}
  <div className="hidden lg:flex flex-col items-center justify-center
                  bg-[linear-gradient(135deg,#0033FE_0%,#0BF1C3_100%)] p-12">
    {/* Logo 与 hero 文案 */}
  </div>

  {/* 右侧：表单 */}
  <div className="flex items-center justify-center p-8">
    <div className="w-full max-w-md">
      {/* 认证表单 */}
    </div>
  </div>
</div>
```

---

## Stack 与 Flex 工具

### 垂直堆叠

```tsx
{/* 保持一致 gap 的 Stack */}
<div className="flex flex-col gap-4">
  <Component />
  <Component />
  <Component />
</div>
```

### 水平簇

```tsx
{/* 可换行的行内元素 */}
<div className="flex flex-wrap items-center gap-2">
  <Badge variant="success" dot>Live</Badge>
  <Badge variant="info">v2.1</Badge>
</div>
```

### 两端分布

```tsx
{/* 左右分布的页头 */}
<div className="flex items-center justify-between">
  <h2 className="text-lg font-semibold">Title</h2>
  <Button size="sm" variant="outline">View all</Button>
</div>
```

---

## 响应式模式

一致使用 Tailwind 断点前缀：

```tsx
{/* Mobile-first：1 列 → 2 列 → 4 列 */}
<div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">

{/* 移动端隐藏，桌面端显示 */}
<aside className="hidden lg:block">

{/* 移动端全宽，桌面端自动宽度 */}
<Button className="w-full sm:w-auto">Submit</Button>

{/* 移动端更小字号 */}
<h1 className="text-2xl font-bold sm:text-3xl lg:text-4xl">
```
