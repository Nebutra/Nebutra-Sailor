{
  "//": "The routes `next build` type-checks, checked before a deploy does it: tsconfig.json excludes src/app (the Vite build shares it), so a type error in a page used to surface only when Fly's build failed.",
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true },
  "include": ["next-env.d.ts", "src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["node_modules", "**/__tests__/**", "**/*.test.ts", "**/*.test.tsx", "src/test/**"]
}
