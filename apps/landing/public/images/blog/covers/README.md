# Blog Cover Images

These covers are bitmap assets generated with the Codex built-in image generation model.

- Runtime image: `*.png`
- Do not recreate these as hand-coded SVG illustrations.

The blog UI should render Sanity `mainImage` first. These covers are only the curated fallback when a post has no CMS image.
