---
title: Vercel
description: Deploy Nebutra to Vercel with zero configuration — automatic environment variable provisioning and edge network optimization.
---

## Overview

Nebutra is built for Vercel. Every app in the monorepo deploys to Vercel with zero additional configuration.

## Deploy from the monorepo

Each app in the monorepo is deployed as a separate Vercel project:

| App | Project name |
|-----|-------------|
| `apps/landing-page` | `nebutra-landing` |
| `apps/web` | `nebutra-web` |
| `apps/api-gateway` | `nebutra-api` |
| `apps/studio` | `nebutra-studio` |
| `apps/docs` | `nebutra-docs` |

### Link each app

```bash
cd apps/landing-page
vercel link

cd ../web
vercel link

# Repeat for each app
```

### Set environment variables

```bash
# Pull env vars for local development
vercel env pull .env.local

# Add a new env var
vercel env add DATABASE_URL production
```

## Turborepo remote cache

Enable shared build cache across CI and local machines:

```bash
npx turbo link
```

This uses your Vercel account as the remote cache backend — cache misses are eliminated across the team.

## Environment variables

Nebutra uses these environment variables on Vercel:

```bash
# Auth
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...
CLERK_SECRET_KEY=sk_live_...
CLERK_WEBHOOK_SECRET=whsec_...

# Database
DATABASE_URL=postgresql://...

# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email
RESEND_API_KEY=re_...

# Redis
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...

# Error tracking
SENTRY_DSN=https://...

# Internal
ADMIN_API_KEY=... # min 32 chars, generate: openssl rand -hex 32
```

## Preview deployments

Every pull request gets an automatic preview URL. Vercel automatically sets the correct `VERCEL_URL` so API calls work in preview environments.

## Vercel WAF

The WAF is enabled by default for all Nebutra deployments. Custom rules are configured in `vercel.json`:

```json
{
  "firewall": {
    "rules": [
      {
        "name": "Block API abuse",
        "active": true,
        "condition": {
          "op": "and",
          "conditions": [
            { "type": "path", "op": "pre", "value": "/api/" },
            { "type": "rate_limit", "requests": 100, "window": 60 }
          ]
        },
        "action": "challenge"
      }
    ]
  }
}
```

## Related

<CardGroup cols={2}>
  <Card title="Rate Limits" icon="gauge" href="/guides/rate-limits">
    API quotas and rate limit configuration.
  </Card>
  <Card title="API Overview" icon="code" href="/api-reference/overview">
    API base URL and conventions.
  </Card>
</CardGroup>
